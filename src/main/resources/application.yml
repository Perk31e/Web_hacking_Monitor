spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_community?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: petuser
    password: Pa$$w0rd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 100MB # 취약점: 파일 크기 제한이 너무 큼
      max-request-size: 100MB
      location: /tmp/uploads # 취약점: 임시 디렉토리 사용

server:
  port: 8080

logging:
  level:
    '[org.springframework.web]': DEBUG
    '[com.petcommunity]': DEBUG
