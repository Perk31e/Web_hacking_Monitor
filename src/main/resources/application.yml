spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_community?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
    username: petuser
    password: Pa$$w0rd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        connection:
          characterEncoding: utf8mb4
          CharSet: utf8mb4
          useUnicode: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
      location: /tmp/uploads

server:
  port: 8080

logging:
  level:
    '[org.springframework.web]': DEBUG
    '[com.petcommunity]': DEBUG